package entities;

import java.awt.*;

public class Ball extends Entity{
    public Ball(int width, int height, int x, int y) { super(width, height, x, y); }

    @Override
    void render(Graphics2D g2d) {

    }
}
